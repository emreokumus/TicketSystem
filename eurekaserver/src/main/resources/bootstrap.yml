spring:
  application:
    name: eureka-server #config serverda bu isimle .yml'i arar
  cloud:
    config:
      #profile: native
      uri: ${CONFIG_SERVER_URL:http://localhost:8888} #config server adresi

